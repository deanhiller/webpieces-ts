/* eslint-disable */
/**
 * The response header the manifest names by CONSTANT.
 *
 * JSON cannot import, so a literal in the manifest would be a copy this rename leaves stale — and the
 * published document would then document a header nothing sets.
 */
export const TRACE_HEADER = 'x-trace-id';
