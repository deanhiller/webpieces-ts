{
    "$schema": "http://json-schema.org/schema",
    "title": "Validate TypeScript Files in src/ Executor",
    "description": "Validates that all .ts files in projects are inside the src/ directory",
    "type": "object",
    "properties": {},
    "required": []
}
