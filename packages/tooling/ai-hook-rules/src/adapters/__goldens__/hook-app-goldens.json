{
  "claude/bash-deny": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": "{\"systemMessage\":\"\\u001b[31;1m🛑 ❌ webpieces ai-hooks blocked this command: git merge main\\u001b[0m\\n\\n[redirect-how-to-merge-main] (1 violation)\\n  L1:  git merge main\\n    → Direct `git merge`/`git rebase` is blocked — AI never runs it, on any branch. Use the gated 3-point flow instead: 'pnpm wp-start-update' → 'pnpm wp-finish-update' when NO PR is open, or 'pnpm wp-start-upsert-pr' → 'pnpm wp-finish-upsert-pr' when a PR IS open (required then — the merge rewrites the branch and the PR must be re-pointed in the same run). If you truly need a raw merge/rebase, ask the HUMAN to run it — and warn them to push back. Full flow: READ <REPO>/.webpieces/instruct-ai/webpieces.git-workflow.md.\\nOn main and just wanted to bring MAIN itself up to date? That is a different question from\\nsyncing a feature branch, and merge/reset is not the answer to it:\\nUpdate main:\\n  pnpm wp-sync-main\\n  To bring main's changes into your feature branch:\\n  \\n  There are exactly TWO flows. Which one you use is decided by ONE question:\\n  is there already an OPEN PR for this branch?\\n  \\n    A. NO PR yet — update-only flow (you are mid-work and just want main's changes):\\n       1. pnpm wp-start-update      ← 3-point merge from main (auto-finalizes if clean)\\n       2. /wp-merge                 ← resolve conflicts (ONLY if step 1 reported any)\\n       3. pnpm wp-finish-update     ← finalize (ONLY on the conflict path)\\n  \\n    B. A PR IS ALREADY OPEN (or you are ready to post one) — PR flow:\\n       1. pnpm wp-start-upsert-pr   ← same 3-point merge, then push\\n       2. /wp-merge                 ← resolve conflicts (ONLY if step 1 reported any)\\n       3. pnpm wp-finish-upsert-pr  ← authoritative build gate, then create/update the PR\\n  \\n  PAIRING IS NOT OPTIONAL — a start and a finish from different flows is not a thing:\\n    wp-start-update    → wp-finish-update\\n    wp-start-upsert-pr → wp-finish-upsert-pr\\n  \\n  If a PR is open you MUST use the upsert-pr pair. The 3-point merge REWRITES this branch (it\\n  squashes onto main and force-pushes a new generation), so the open PR's history is blown\\n  away and has to be re-pointed in the SAME run. The update-only pair never touches the PR, so\\n  running it with a PR open would strand that PR on the OLD branch generation — which is why\\n  wp-start-update refuses outright when it finds an open PR.\\n  \\n  Either flow does a 3-point merge, which keeps the fork point a pure main commit — what the build\\n  gate's --base and the PR review diff are computed from; a raw merge/rebase destroys it. The\\n  gated commands merge internally as child processes this hook never sees.\\n  \\n  Only wanted to LOOK (am I behind main? would it fast-forward?) — none of the below mutate:\\n    git fetch origin main                                 ← refresh the remote ref (no merge)\\n    git merge-base --is-ancestor origin/main HEAD         ← exit 0 = already contains main\\n    git rev-list --left-right --count origin/main...HEAD  ← prints \\\"<behind>  <ahead>\\\"\\n    git log --oneline HEAD..origin/main                   ← what main has that you do not\\n    git diff --stat origin/main...HEAD                    ← what you changed since the fork point\\n    cat .webpieces/main-sync-status.json                  ← the tooling's own answer, incl.\\n                                                            the files predicted to conflict\\n  `git merge --ff-only` is NOT a look — it MUTATES on success (that is the whole point of a\\n  fast-forward), so it is blocked like every other merge. Never use it as a probe.\\n  \\n  READ the instruct-ai git-workflow doc at the absolute path on the violation line above — the full\\n  flow (incl. worktrees), the fork-point invariant, why you must\\n  never sync a branch something is ACTIVELY holding (liveness, not authorship — a finished agent\\n  holds nothing), and the exact words to warn a human with when you ask them to run a merge.\\n  Add to memory: never run a raw `git merge`/`git rebase` — use this repo's gated sync flow and\\n  re-read its git-workflow doc each time rather than recalling it (regenerated per repo/version).\\n\\nThis is a pre-run check. Follow the fix above, then retry the command.\\n\",\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"❌ webpieces ai-hooks blocked this command: git merge main\\n\\n[redirect-how-to-merge-main] (1 violation)\\n  L1:  git merge main\\n    → Direct `git merge`/`git rebase` is blocked — AI never runs it, on any branch. Use the gated 3-point flow instead: 'pnpm wp-start-update' → 'pnpm wp-finish-update' when NO PR is open, or 'pnpm wp-start-upsert-pr' → 'pnpm wp-finish-upsert-pr' when a PR IS open (required then — the merge rewrites the branch and the PR must be re-pointed in the same run). If you truly need a raw merge/rebase, ask the HUMAN to run it — and warn them to push back. Full flow: READ <REPO>/.webpieces/instruct-ai/webpieces.git-workflow.md.\\nOn main and just wanted to bring MAIN itself up to date? That is a different question from\\nsyncing a feature branch, and merge/reset is not the answer to it:\\nUpdate main:\\n  pnpm wp-sync-main\\n  To bring main's changes into your feature branch:\\n  \\n  There are exactly TWO flows. Which one you use is decided by ONE question:\\n  is there already an OPEN PR for this branch?\\n  \\n    A. NO PR yet — update-only flow (you are mid-work and just want main's changes):\\n       1. pnpm wp-start-update      ← 3-point merge from main (auto-finalizes if clean)\\n       2. /wp-merge                 ← resolve conflicts (ONLY if step 1 reported any)\\n       3. pnpm wp-finish-update     ← finalize (ONLY on the conflict path)\\n  \\n    B. A PR IS ALREADY OPEN (or you are ready to post one) — PR flow:\\n       1. pnpm wp-start-upsert-pr   ← same 3-point merge, then push\\n       2. /wp-merge                 ← resolve conflicts (ONLY if step 1 reported any)\\n       3. pnpm wp-finish-upsert-pr  ← authoritative build gate, then create/update the PR\\n  \\n  PAIRING IS NOT OPTIONAL — a start and a finish from different flows is not a thing:\\n    wp-start-update    → wp-finish-update\\n    wp-start-upsert-pr → wp-finish-upsert-pr\\n  \\n  If a PR is open you MUST use the upsert-pr pair. The 3-point merge REWRITES this branch (it\\n  squashes onto main and force-pushes a new generation), so the open PR's history is blown\\n  away and has to be re-pointed in the SAME run. The update-only pair never touches the PR, so\\n  running it with a PR open would strand that PR on the OLD branch generation — which is why\\n  wp-start-update refuses outright when it finds an open PR.\\n  \\n  Either flow does a 3-point merge, which keeps the fork point a pure main commit — what the build\\n  gate's --base and the PR review diff are computed from; a raw merge/rebase destroys it. The\\n  gated commands merge internally as child processes this hook never sees.\\n  \\n  Only wanted to LOOK (am I behind main? would it fast-forward?) — none of the below mutate:\\n    git fetch origin main                                 ← refresh the remote ref (no merge)\\n    git merge-base --is-ancestor origin/main HEAD         ← exit 0 = already contains main\\n    git rev-list --left-right --count origin/main...HEAD  ← prints \\\"<behind>  <ahead>\\\"\\n    git log --oneline HEAD..origin/main                   ← what main has that you do not\\n    git diff --stat origin/main...HEAD                    ← what you changed since the fork point\\n    cat .webpieces/main-sync-status.json                  ← the tooling's own answer, incl.\\n                                                            the files predicted to conflict\\n  `git merge --ff-only` is NOT a look — it MUTATES on success (that is the whole point of a\\n  fast-forward), so it is blocked like every other merge. Never use it as a probe.\\n  \\n  READ the instruct-ai git-workflow doc at the absolute path on the violation line above — the full\\n  flow (incl. worktrees), the fork-point invariant, why you must\\n  never sync a branch something is ACTIVELY holding (liveness, not authorship — a finished agent\\n  holds nothing), and the exact words to warn a human with when you ask them to run a merge.\\n  Add to memory: never run a raw `git merge`/`git rebase` — use this repo's gated sync flow and\\n  re-read its git-workflow doc each time rather than recalling it (regenerated per repo/version).\\n\\nThis is a pre-run check. Follow the fix above, then retry the command.\\n\"}}\n"
  },
  "claude/bash-allow": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": ""
  },
  "claude/read-allow": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": ""
  },
  "claude/write-deny": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": "{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"❌ webpieces ai-hooks blocked this write: src/foo.js\\n\\n[no-js-files] (1 violation)\\n  L1:  src/foo.js\\n    → Writing .js/.jsx files is not allowed. Use .ts/.tsx instead.\\n  Pick one:\\n  Fix Option 1: (preferred) Write a .ts or .tsx file instead of .js/.jsx.\\n  Fix Option 2: If this path must be .js (generated/legacy), add it to no-js-files.allowedPaths in webpieces.config.json\\n\\nThis is a pre-write check. Fix and retry the Write/Edit.\\n\"}}\n"
  },
  "claude/edit-deny": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": "{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"❌ webpieces ai-hooks blocked this write: scripts/ok.ts\\n\\n[no-destructure] (1 violation)\\n  L1:  const { a } = b;\\n    → Destructuring pattern on a call result.\\n  Pick one:\\n  Fix Option 1: (preferred) Assign the whole result instead: const obj = methodCall(); then pass obj around or use obj.x.\\n  Fix Option 2: If this whole tree is destructuring by construction (a React/React Native component or hook tree), add a glob to no-destructure.allowedPaths in webpieces.config.json\\n  🔒 The team disabled escaping via webpieces-disable for this rule (disableAllowed:false) — it must be followed.\\n\\nThis is a pre-write check. Fix and retry the Write/Edit.\\n\"}}\n"
  },
  "claude/multiedit-deny": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": "{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"❌ webpieces ai-hooks blocked this write: scripts/ok.ts\\n\\n[no-destructure] (1 violation)\\n  L1:  const { a } = b;\\n    → Destructuring pattern on a call result.\\n  Pick one:\\n  Fix Option 1: (preferred) Assign the whole result instead: const obj = methodCall(); then pass obj around or use obj.x.\\n  Fix Option 2: If this whole tree is destructuring by construction (a React/React Native component or hook tree), add a glob to no-destructure.allowedPaths in webpieces.config.json\\n  🔒 The team disabled escaping via webpieces-disable for this rule (disableAllowed:false) — it must be followed.\\n\\nThis is a pre-write check. Fix and retry the Write/Edit.\\n\"}}\n"
  },
  "claude/write-allow": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": ""
  },
  "claude/malformed": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": "{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Malformed hook input from Claude Code stdin: Unexpected token 'o', \\\"not json at all\\\" is not valid JSON\"}}\n"
  },
  "claude/crash": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": "{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Cannot load custom rule '<REPO>/wprules/crash.js'\"}}\n"
  },
  "codex/bash-deny": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": "{\"systemMessage\":\"\\u001b[31;1m🛑 ❌ webpieces ai-hooks blocked this command: git merge main\\u001b[0m\\n\\n[redirect-how-to-merge-main] (1 violation)\\n  L1:  git merge main\\n    → Direct `git merge`/`git rebase` is blocked — AI never runs it, on any branch. Use the gated 3-point flow instead: 'pnpm wp-start-update' → 'pnpm wp-finish-update' when NO PR is open, or 'pnpm wp-start-upsert-pr' → 'pnpm wp-finish-upsert-pr' when a PR IS open (required then — the merge rewrites the branch and the PR must be re-pointed in the same run). If you truly need a raw merge/rebase, ask the HUMAN to run it — and warn them to push back. Full flow: READ <REPO>/.webpieces/instruct-ai/webpieces.git-workflow.md.\\nOn main and just wanted to bring MAIN itself up to date? That is a different question from\\nsyncing a feature branch, and merge/reset is not the answer to it:\\nUpdate main:\\n  pnpm wp-sync-main\\n  To bring main's changes into your feature branch:\\n  \\n  There are exactly TWO flows. Which one you use is decided by ONE question:\\n  is there already an OPEN PR for this branch?\\n  \\n    A. NO PR yet — update-only flow (you are mid-work and just want main's changes):\\n       1. pnpm wp-start-update      ← 3-point merge from main (auto-finalizes if clean)\\n       2. /wp-merge                 ← resolve conflicts (ONLY if step 1 reported any)\\n       3. pnpm wp-finish-update     ← finalize (ONLY on the conflict path)\\n  \\n    B. A PR IS ALREADY OPEN (or you are ready to post one) — PR flow:\\n       1. pnpm wp-start-upsert-pr   ← same 3-point merge, then push\\n       2. /wp-merge                 ← resolve conflicts (ONLY if step 1 reported any)\\n       3. pnpm wp-finish-upsert-pr  ← authoritative build gate, then create/update the PR\\n  \\n  PAIRING IS NOT OPTIONAL — a start and a finish from different flows is not a thing:\\n    wp-start-update    → wp-finish-update\\n    wp-start-upsert-pr → wp-finish-upsert-pr\\n  \\n  If a PR is open you MUST use the upsert-pr pair. The 3-point merge REWRITES this branch (it\\n  squashes onto main and force-pushes a new generation), so the open PR's history is blown\\n  away and has to be re-pointed in the SAME run. The update-only pair never touches the PR, so\\n  running it with a PR open would strand that PR on the OLD branch generation — which is why\\n  wp-start-update refuses outright when it finds an open PR.\\n  \\n  Either flow does a 3-point merge, which keeps the fork point a pure main commit — what the build\\n  gate's --base and the PR review diff are computed from; a raw merge/rebase destroys it. The\\n  gated commands merge internally as child processes this hook never sees.\\n  \\n  Only wanted to LOOK (am I behind main? would it fast-forward?) — none of the below mutate:\\n    git fetch origin main                                 ← refresh the remote ref (no merge)\\n    git merge-base --is-ancestor origin/main HEAD         ← exit 0 = already contains main\\n    git rev-list --left-right --count origin/main...HEAD  ← prints \\\"<behind>  <ahead>\\\"\\n    git log --oneline HEAD..origin/main                   ← what main has that you do not\\n    git diff --stat origin/main...HEAD                    ← what you changed since the fork point\\n    cat .webpieces/main-sync-status.json                  ← the tooling's own answer, incl.\\n                                                            the files predicted to conflict\\n  `git merge --ff-only` is NOT a look — it MUTATES on success (that is the whole point of a\\n  fast-forward), so it is blocked like every other merge. Never use it as a probe.\\n  \\n  READ the instruct-ai git-workflow doc at the absolute path on the violation line above — the full\\n  flow (incl. worktrees), the fork-point invariant, why you must\\n  never sync a branch something is ACTIVELY holding (liveness, not authorship — a finished agent\\n  holds nothing), and the exact words to warn a human with when you ask them to run a merge.\\n  Add to memory: never run a raw `git merge`/`git rebase` — use this repo's gated sync flow and\\n  re-read its git-workflow doc each time rather than recalling it (regenerated per repo/version).\\n\\nThis is a pre-run check. Follow the fix above, then retry the command.\\n\",\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"❌ webpieces ai-hooks blocked this command: git merge main\\n\\n[redirect-how-to-merge-main] (1 violation)\\n  L1:  git merge main\\n    → Direct `git merge`/`git rebase` is blocked — AI never runs it, on any branch. Use the gated 3-point flow instead: 'pnpm wp-start-update' → 'pnpm wp-finish-update' when NO PR is open, or 'pnpm wp-start-upsert-pr' → 'pnpm wp-finish-upsert-pr' when a PR IS open (required then — the merge rewrites the branch and the PR must be re-pointed in the same run). If you truly need a raw merge/rebase, ask the HUMAN to run it — and warn them to push back. Full flow: READ <REPO>/.webpieces/instruct-ai/webpieces.git-workflow.md.\\nOn main and just wanted to bring MAIN itself up to date? That is a different question from\\nsyncing a feature branch, and merge/reset is not the answer to it:\\nUpdate main:\\n  pnpm wp-sync-main\\n  To bring main's changes into your feature branch:\\n  \\n  There are exactly TWO flows. Which one you use is decided by ONE question:\\n  is there already an OPEN PR for this branch?\\n  \\n    A. NO PR yet — update-only flow (you are mid-work and just want main's changes):\\n       1. pnpm wp-start-update      ← 3-point merge from main (auto-finalizes if clean)\\n       2. /wp-merge                 ← resolve conflicts (ONLY if step 1 reported any)\\n       3. pnpm wp-finish-update     ← finalize (ONLY on the conflict path)\\n  \\n    B. A PR IS ALREADY OPEN (or you are ready to post one) — PR flow:\\n       1. pnpm wp-start-upsert-pr   ← same 3-point merge, then push\\n       2. /wp-merge                 ← resolve conflicts (ONLY if step 1 reported any)\\n       3. pnpm wp-finish-upsert-pr  ← authoritative build gate, then create/update the PR\\n  \\n  PAIRING IS NOT OPTIONAL — a start and a finish from different flows is not a thing:\\n    wp-start-update    → wp-finish-update\\n    wp-start-upsert-pr → wp-finish-upsert-pr\\n  \\n  If a PR is open you MUST use the upsert-pr pair. The 3-point merge REWRITES this branch (it\\n  squashes onto main and force-pushes a new generation), so the open PR's history is blown\\n  away and has to be re-pointed in the SAME run. The update-only pair never touches the PR, so\\n  running it with a PR open would strand that PR on the OLD branch generation — which is why\\n  wp-start-update refuses outright when it finds an open PR.\\n  \\n  Either flow does a 3-point merge, which keeps the fork point a pure main commit — what the build\\n  gate's --base and the PR review diff are computed from; a raw merge/rebase destroys it. The\\n  gated commands merge internally as child processes this hook never sees.\\n  \\n  Only wanted to LOOK (am I behind main? would it fast-forward?) — none of the below mutate:\\n    git fetch origin main                                 ← refresh the remote ref (no merge)\\n    git merge-base --is-ancestor origin/main HEAD         ← exit 0 = already contains main\\n    git rev-list --left-right --count origin/main...HEAD  ← prints \\\"<behind>  <ahead>\\\"\\n    git log --oneline HEAD..origin/main                   ← what main has that you do not\\n    git diff --stat origin/main...HEAD                    ← what you changed since the fork point\\n    cat .webpieces/main-sync-status.json                  ← the tooling's own answer, incl.\\n                                                            the files predicted to conflict\\n  `git merge --ff-only` is NOT a look — it MUTATES on success (that is the whole point of a\\n  fast-forward), so it is blocked like every other merge. Never use it as a probe.\\n  \\n  READ the instruct-ai git-workflow doc at the absolute path on the violation line above — the full\\n  flow (incl. worktrees), the fork-point invariant, why you must\\n  never sync a branch something is ACTIVELY holding (liveness, not authorship — a finished agent\\n  holds nothing), and the exact words to warn a human with when you ask them to run a merge.\\n  Add to memory: never run a raw `git merge`/`git rebase` — use this repo's gated sync flow and\\n  re-read its git-workflow doc each time rather than recalling it (regenerated per repo/version).\\n\\nThis is a pre-run check. Follow the fix above, then retry the command.\\n\"}}\n"
  },
  "codex/bash-allow": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": ""
  },
  "codex/read-allow": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": ""
  },
  "codex/malformed": {
    "capturedFrom": "0.4.696",
    "exitCode": 0,
    "stdout": "{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Malformed hook input from Claude Code stdin: Unexpected token 'b', \\\"{\\\"turn_id\\\": broken\\\" is not valid JSON\"}}\n"
  },
  "codex/apply-patch-deny": {
    "capturedFrom": "main (new surface in #723 — 0.4.696 has no Codex path)",
    "exitCode": 0,
    "stdout": "{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"❌ webpieces ai-hooks blocked this write: src/foo.js\\n\\n[no-js-files] (1 violation)\\n  L1:  src/foo.js\\n    → Writing .js/.jsx files is not allowed. Use .ts/.tsx instead.\\n  Pick one:\\n  Fix Option 1: (preferred) Write a .ts or .tsx file instead of .js/.jsx.\\n  Fix Option 2: If this path must be .js (generated/legacy), add it to no-js-files.allowedPaths in webpieces.config.json\\n\\nThis is a pre-write check. Fix and retry the Write/Edit.\\n\"}}\n"
  },
  "codex/apply-patch-allow": {
    "capturedFrom": "main (new surface in #723 — 0.4.696 has no Codex path)",
    "exitCode": 0,
    "stdout": ""
  },
  "codex/crash": {
    "capturedFrom": "main (new surface in #723 — 0.4.696 has no Codex path)",
    "exitCode": 0,
    "stdout": "{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Cannot load custom rule '<REPO>/wprules/crash.js'\"}}\n"
  }
}
