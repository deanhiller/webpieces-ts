{
    "executors": {
        "help": {
            "implementation": "./src/executors/help/executor",
            "schema": "./src/executors/help/schema.json",
            "description": "Display help for @webpieces/dev-config commands and targets"
        },
        "validate-eslint-sync": {
            "implementation": "./src/executors/validate-eslint-sync/executor",
            "schema": "./src/executors/validate-eslint-sync/schema.json",
            "description": "Validate that workspace and template eslint.webpieces.config.mjs files have identical rules"
        },
        "validate-versions-locked": {
            "implementation": "./src/executors/validate-versions-locked/executor",
            "schema": "./src/executors/validate-versions-locked/schema.json",
            "description": "Validate package.json versions are locked (no semver ranges) and npm ci compatible"
        }
    }
}
