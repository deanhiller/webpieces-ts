{
    "aiInstructions": "Each project's shortDescription is only a summary. BEFORE adding code to a project, read its responsibilitiesFile for the full responsibilities (what belongs in that project and what does not), and read its designFile to understand the DI design before reading the code. Use the entries in `commands` to regenerate these files or display any of the graphs in a browser.",
    "commands": {
        "regenerateArchitecture": "pnpm nx run architecture:generate — rewrites architecture/dependencies.json and architecture/runtime-dependencies.json; run after adding/removing project dependencies",
        "visualizeArchitecture": "pnpm nx run architecture:visualize — opens the monorepo dependency graph (this file) as HTML in a browser",
        "visualizeRuntimeArchitecture": "pnpm nx run architecture:visualize-runtime — opens the runtime microservice call graph as HTML in a browser",
        "regenerateDesigns": "pnpm nx run-many --target=di-graph-generate — rewrites every project's design.json/design.md (also runs automatically on every build)",
        "visualizeDesign": "pnpm wp-design-visualize <project> — opens a project's DI designs (its designFile) as HTML, one graph per controller with the controller at the top; no args = interactive picker"
    },
    "projects": {
        "ai-hook-rules": {
            "level": 1,
            "framework": ["node"],
            "role": "lib",
            "shortDescription": "Edit-time validation engine for AI coding agents. Intercepts proposed file writes/edits (and Bash git/PR guards) before they land via Claude Code PreToolUse and openclaw before_tool_call adapters, running configurable rules and rejecting bad output with educational fix hints.",
            "responsibilitiesFile": "packages/tooling/ai-hook-rules/responsibilities.md",
            "dependsOn": [
                "rules-config"
            ]
        },
        "angular-site": {
            "level": 3,
            "framework": ["angular"],
            "role": "client",
            "shortDescription": "Angular single-page frontend demonstrating the webpieces browser client. It injects `SaveApi`/`PublicApi` as generated HTTP clients from `client-server-api` and calls the client-server service, wiring a HeaderRegistry from `company-core` for context header transfer.",
            "responsibilitiesFile": "apps/app-example/angular-site/responsibilities.md",
            "designFile": "apps/app-example/angular-site/design.json",
            "apiRelations": {
                "client-server-api": {
                    "kind": "uses",
                    "implements": [],
                    "uses": [
                        {
                            "api": "PublicApi",
                            "type": "rpc"
                        },
                        {
                            "api": "SaveApi",
                            "type": "rpc"
                        }
                    ]
                }
            },
            "dependsOn": [
                "client-server-api",
                "company-core",
                "http-client-browser"
            ]
        },
        "app-example-e2e": {
            "level": 7,
            "framework": ["express"],
            "role": "server",
            "shortDescription": "End-to-end test project for the two-service example. Boots both `client-server` and `server2` on test ports with real HTTP between them and asserts the magic context (correlation/transaction id, per-hop request-id chain, tenant) flows through both servers' logs, with secured headers masked.",
            "responsibilitiesFile": "apps/app-example/e2e/responsibilities.md",
            "dependsOn": [
                "client-server",
                "server2"
            ]
        },
        "bunyan": {
            "level": 2,
            "framework": ["node"],
            "role": "lib",
            "shortDescription": "Node-only bunyan backends for the webpieces logging seam: a `BunyanConsoleFactory` (local pretty text) and a `BunyanGcpFactory` (Cloud Logging via `@google-cloud/logging-bunyan`), both `LoggerFactory` impls that enrich every line with the logged `HeaderRegistry` keys.",
            "responsibilitiesFile": "packages/logging/bunyan/responsibilities.md",
            "dependsOn": [
                "core-context"
            ]
        },
        "client-server": {
            "level": 6,
            "framework": ["express"],
            "role": "server",
            "shortDescription": "Runnable example webpieces service (port 8200) implementing the `client-server-api` contract (SaveController, PublicController). It also acts as a client that calls `server2` over real HTTP, demonstrating DI modules, filters, and magic-context header transfer.",
            "responsibilitiesFile": "apps/app-example/client-server/responsibilities.md",
            "designFile": "apps/app-example/client-server/design.json",
            "apiRelations": {
                "client-server-api": {
                    "kind": "implements",
                    "implements": [
                        {
                            "api": "PublicApi",
                            "type": "rpc"
                        },
                        {
                            "api": "SaveApi",
                            "type": "rpc"
                        },
                        {
                            "api": "SecureApi",
                            "type": "rpc"
                        }
                    ],
                    "uses": []
                },
                "server2-api": {
                    "kind": "uses",
                    "implements": [],
                    "uses": [
                        {
                            "api": "Server2Api",
                            "type": "rpc"
                        }
                    ]
                }
            },
            "dependsOn": [
                "client-server-api",
                "company-svc-core",
                "core-mock",
                "http-client-node",
                "server2-api"
            ]
        },
        "client-server-api": {
            "level": 1,
            "framework": ["browser","node"],
            "role": "api-lib",
            "shortDescription": "Shared API contract for the client-server service: `SaveApi`/`PublicApi` abstract classes plus request/response DTOs, annotated with webpieces routing/auth decorators. Single source of truth implemented server-side and consumed by browser clients.",
            "responsibilitiesFile": "apps/app-example/client-server-api/responsibilities.md",
            "dependsOn": [
                "core-util"
            ]
        },
        "cloudtasks-client": {
            "level": 3,
            "framework": ["node"],
            "role": "designed-lib",
            "shortDescription": "Cloud Tasks enqueue client generated from a shared `@PubSub` API contract — the fire-and-forget twin of http-client. Owns the enqueue proxy, the scheduler, and the TaskInvoker transports (GCP + in-memory), both of which deliver a task over real HTTP.",
            "responsibilitiesFile": "packages/cloud/cloudtasks-client/responsibilities.md",
            "designFile": "packages/cloud/cloudtasks-client/design.json",
            "dependsOn": [
                "gcp-identity"
            ]
        },
        "code-rules": {
            "level": 2,
            "framework": ["node"],
            "role": "app",
            "shortDescription": "Build-time code validation gate. Standalone (no Nx dependency) CLI that validates new and modified code against the shared rule set over a git diff, enforcing method/file limits, return types, no-any, DI-token, exception, controller-naming and related rules in CI.",
            "responsibilitiesFile": "packages/tooling/code-rules/responsibilities.md",
            "dependsOn": [
                "core-context",
                "rules-config"
            ]
        },
        "company-core": {
            "level": 1,
            "framework": ["browser","node"],
            "role": "lib",
            "shortDescription": "Company-wide shared core library, browser-safe, brought in by ALL example projects. Defines the second-tier `CompanyHeaders` (tenant id, api version) as `PlatformHeader` constants that servers bind and browsers register — the company layer between framework core headers and app headers.",
            "responsibilitiesFile": "apps/app-example/company-core/responsibilities.md",
            "dependsOn": [
                "core-util"
            ]
        },
        "company-svc-core": {
            "level": 5,
            "framework": ["express"],
            "role": "lib",
            "shortDescription": "Company-wide shared SERVER core (node-only, `framework:express`). Holds the single shared server bootstrap and shared server DI so every express service starts the same way. It is the node-only counterpart to the browser-safe `company-core` and is NOT imported by Angular.",
            "responsibilitiesFile": "apps/app-example/company-svc-core/responsibilities.md",
            "dependsOn": [
                "company-core",
                "http-server"
            ]
        },
        "core-context": {
            "level": 1,
            "framework": ["node"],
            "role": "lib",
            "shortDescription": "AsyncLocalStorage-based request-scoped context (a TypeScript port of Java webpieces' ThreadLocal Context/MDC). Exposes the RequestContext singleton to run(), get/put/remove/clear, and copy/restore key-value data automatically across async boundaries.",
            "responsibilitiesFile": "packages/core/core-context/responsibilities.md",
            "dependsOn": [
                "core-util"
            ]
        },
        "core-mock": {
            "level": 0,
            "framework": ["browser","node"],
            "role": "lib",
            "shortDescription": "Typed mock framework for feature tests (port of Java webpieces MockSuperclass). `createMock<T>()` returns a Proxy implementing any api plus a `.mock` control facade for the prime/assert vocabulary; `MockHandler` is the underlying queue/default/drain engine.",
            "responsibilitiesFile": "packages/core/core-mock/responsibilities.md",
            "dependsOn": []
        },
        "core-util": {
            "level": 0,
            "framework": ["browser","node"],
            "role": "lib",
            "shortDescription": "Lowest-level, zero-dependency utilities shared across webpieces (browser and Node). Provides `toError()` for standardized catch handling, plus the `Header` interface and `ContextKey` class that higher packages build request-context and HTTP-header abstractions on.",
            "responsibilitiesFile": "packages/core/core-util/responsibilities.md",
            "dependsOn": []
        },
        "eslint-rules": {
            "level": 1,
            "framework": ["node"],
            "role": "lib",
            "shortDescription": "Custom ESLint plugin exporting WebPieces lint rules — architecture dependency boundaries, exception/catch-error patterns, method/file size limits, no-JsonProperty-primitive, and Angular template rules — consumed by the shared eslint config and the nx-webpieces build validators.",
            "responsibilitiesFile": "packages/tooling/eslint-rules/responsibilities.md",
            "dependsOn": [
                "rules-config"
            ]
        },
        "gcp-identity": {
            "level": 2,
            "framework": ["node"],
            "role": "lib",
            "shortDescription": "GCP runtime identity for Node services: project/region/service-account metadata, Cloud Run URL derivation, and OIDC token mint/verify — with deterministic localhost/dev fallbacks off-GCP so tests and local dev need no GCP.",
            "responsibilitiesFile": "packages/cloud/gcp-identity/responsibilities.md",
            "dependsOn": [
                "core-context"
            ]
        },
        "http-client-browser": {
            "level": 2,
            "framework": ["browser"],
            "role": "designed-lib",
            "shortDescription": "The browser-side HTTP client: generates type-safe clients from the SAME API contract the server implements. Deliberately DI-free, because it may be bundled by React just as easily as by Angular, so it ships no `inversify` and no node-only `core-context`.",
            "responsibilitiesFile": "packages/http/http-client-browser/responsibilities.md",
            "designFile": "packages/http/http-client-browser/design.json",
            "dependsOn": [
                "http-client-core"
            ]
        },
        "http-client-core": {
            "level": 1,
            "framework": ["browser","node"],
            "role": "lib",
            "shortDescription": "The isomorphic engine of the webpieces HTTP client: reads an API contract's decorators and turns each method call into an HTTP request. It takes no position on where the magic context comes from or whether a DI container exists, so `http-client-node` and `http-client-browser` both build on it.",
            "responsibilitiesFile": "packages/http/http-client-core/responsibilities.md",
            "dependsOn": [
                "core-util"
            ]
        },
        "http-client-node": {
            "level": 3,
            "framework": ["node"],
            "role": "designed-lib",
            "shortDescription": "The server-side HTTP client: generates type-safe clients from the SAME API contract the callee's controller implements. Node-only, so it is fully inversify-wired, reads the magic context straight out of the `RequestContext`, and mints its own delivery auth.",
            "responsibilitiesFile": "packages/http/http-client-node/responsibilities.md",
            "designFile": "packages/http/http-client-node/design.json",
            "dependsOn": [
                "gcp-identity",
                "http-client-core"
            ]
        },
        "http-routing": {
            "level": 3,
            "framework": ["node"],
            "role": "designed-lib",
            "shortDescription": "Server-side routing layer: `@Controller`/DI decorators, `WebAppMeta`/`Routes`/`RouteBuilder` wiring, `RouteDefinition`/`FilterDefinition`, `MethodMeta`, `FilterMatcher` (glob-based filter-to-route matching), and `ApiRoutingFactory` that maps http-api decorators to controller handlers.",
            "responsibilitiesFile": "packages/http/http-routing/responsibilities.md",
            "designFile": "packages/http/http-routing/design.json",
            "dependsOn": [
                "gcp-identity"
            ]
        },
        "http-server": {
            "level": 4,
            "framework": ["express"],
            "role": "lib",
            "shortDescription": "Server runtime that assembles the HTTP layer: bootstraps the Inversify DI container, builds routes/filter chains from `WebAppMeta`, serves them over Express, ships built-in filters (Context, LogApi, Recording, ServiceAuth), the in-process test client, and the test-case recorder.",
            "responsibilitiesFile": "packages/http/http-server/responsibilities.md",
            "dependsOn": [
                "http-routing"
            ]
        },
        "legacy-server": {
            "level": 6,
            "framework": ["express"],
            "role": "server",
            "shortDescription": "Example of INCREMENTAL webpieces adoption: a pre-existing express app (own routes, untouched) with the webpieces api → filters → controller pipeline bolted on via `WebpiecesRouteCreator`, using the SAME shared `setupCompanyRuntime` startup as the greenfield server. Proven by an integration test.",
            "responsibilitiesFile": "apps/app-example/legacy-server/responsibilities.md",
            "apiRelations": {
                "client-server-api": {
                    "kind": "implements",
                    "implements": [
                        {
                            "api": "PublicApi",
                            "type": "rpc"
                        },
                        {
                            "api": "SaveApi",
                            "type": "rpc"
                        }
                    ],
                    "uses": []
                },
                "server2-api": {
                    "kind": "uses",
                    "implements": [],
                    "uses": [
                        {
                            "api": "Server2Api",
                            "type": "rpc"
                        }
                    ]
                }
            },
            "dependsOn": [
                "client-server-api",
                "company-svc-core",
                "http-client-node",
                "server2-api"
            ]
        },
        "nx-webpieces-rules": {
            "level": 3,
            "framework": ["node"],
            "role": "lib",
            "shortDescription": "Nx inference plugin that auto-wires webpieces build gates with no manual project.json edits: architecture and runtime graph generators/validators, the Inversify DI design.json/design.md generator, a per-project circular-import gate, and many validate-* code-style and size executors.",
            "responsibilitiesFile": "packages/tooling/nx-webpieces-rules/responsibilities.md",
            "dependsOn": [
                "ai-hook-rules",
                "code-rules",
                "eslint-rules",
                "pr-gate"
            ]
        },
        "pr-gate": {
            "level": 2,
            "framework": ["node"],
            "role": "app",
            "shortDescription": "Standalone CLIs for the gated PR workflow — start/finish-upsert-pr, start/finish-update (update from main), merge start/end — plus the red/yellow/green PR dashboard that computes merge-gate results and counts newly added rule disables.",
            "responsibilitiesFile": "packages/tooling/pr-gate/responsibilities.md",
            "dependsOn": [
                "core-context",
                "rules-config"
            ]
        },
        "rules-config": {
            "level": 0,
            "framework": ["node"],
            "role": "lib",
            "shortDescription": "Shared config/schema layer: loads and validates webpieces.config.json, defines every rule's typed config, mode unions, defaults, path-exclusion and diff-scope helpers, plus PR-gate and main-sync state. Single source of truth consumed by ai-hook-rules, code-rules, and nx-webpieces-rules.",
            "responsibilitiesFile": "packages/tooling/rules-config/responsibilities.md",
            "dependsOn": []
        },
        "server2": {
            "level": 6,
            "framework": ["express"],
            "role": "server",
            "shortDescription": "Runnable downstream example service (port 8202) implementing the `server2-api` contract. Server2Controller echoes received magic-context headers (request-id chain, previous-id, tenant) back in its response so callers and the e2e test can prove end-to-end context transfer.",
            "responsibilitiesFile": "apps/app-example/server2/responsibilities.md",
            "designFile": "apps/app-example/server2/design.json",
            "apiRelations": {
                "server2-api": {
                    "kind": "implements",
                    "implements": [
                        {
                            "api": "Server2Api",
                            "type": "rpc"
                        }
                    ],
                    "uses": []
                }
            },
            "dependsOn": [
                "company-svc-core",
                "server2-api"
            ]
        },
        "server2-api": {
            "level": 1,
            "framework": ["browser","node"],
            "role": "api-lib",
            "shortDescription": "Shared API contract for server2: the `Server2Api` abstract class (fetchValue) plus its request/response DTOs, annotated with webpieces routing/auth decorators. Implemented by `server2` and consumed by `client-server` as an HTTP client.",
            "responsibilitiesFile": "apps/app-example/server2-api/responsibilities.md",
            "dependsOn": [
                "core-util"
            ]
        },
        "winston": {
            "level": 2,
            "framework": ["node"],
            "role": "lib",
            "shortDescription": "Node-only winston backends for the webpieces logging seam: a `WinstonConsoleFactory` (local pretty) and a `WinstonGcpFactory` (Cloud Run stdout JSON with a level→severity map), both `LoggerFactory` impls that enrich every line with the logged `HeaderRegistry` context keys.",
            "responsibilitiesFile": "packages/logging/winston/responsibilities.md",
            "dependsOn": [
                "core-context"
            ]
        }
    }
}
